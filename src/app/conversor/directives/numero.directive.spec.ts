import { NumeroDirective } from './numero.directive';

describe('NumeroDirective', () => {
  it('should create an instance', () => {
    const directive = rew NumeroDirective();
    expect(directive).toBeTruthy();
  });
});
